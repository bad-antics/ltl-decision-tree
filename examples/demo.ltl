// examples/demo.ltl — runnable demo for ltl_decision_tree

import std::io
use ltl_decision_tree as lib

fn main() {
    io::println("ltl_decision_tree demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}